# Auto-generated by .github/workflows/build-deploy.yml on each
# main-branch push — the annotation changes with every VERSION
# so Flux rolls the pod, which picks up the latest :latest
# image and the current platform-version ConfigMap value.
# Do not edit by hand — edits will be overwritten.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-api
  namespace: platform
spec:
  template:
    metadata:
      annotations:
        platform.phoenix-host.net/deploy-rev: "0.0.0-58380bd"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-panel
  namespace: platform
spec:
  template:
    metadata:
      annotations:
        platform.phoenix-host.net/deploy-rev: "0.0.0-58380bd"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-panel
  namespace: platform
spec:
  template:
    metadata:
      annotations:
        platform.phoenix-host.net/deploy-rev: "0.0.0-58380bd"

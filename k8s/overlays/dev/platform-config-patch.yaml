apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config
  namespace: platform
data:
  environment: "development"
  log-level: "debug"
  default-storage-class: "local-path"
  ingress-base-domain: "k8s-platform.local-dev"
  cluster-issuer-name: "selfsigned-issuer"
  admin-url: "http://admin.k8s-platform.local-dev"
  client-url: "http://client.k8s-platform.local-dev"
  cors-origins: "http://admin.k8s-platform.local-dev,http://client.k8s-platform.local-dev,http://localhost:5173,http://localhost:5174"

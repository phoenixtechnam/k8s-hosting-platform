# Dev-only secrets — hardcoded values safe for local development only.
# Production creates these out-of-band via sealed-secrets or external-secrets.
apiVersion: v1
kind: Secret
metadata:
  name: platform-db-credentials
  namespace: platform
type: Opaque
stringData:
  password: local-dev-password
  url: "postgresql://platform:local-dev-password@postgres.platform.svc.cluster.local:5432/hosting_platform"
---
apiVersion: v1
kind: Secret
metadata:
  name: platform-jwt-secret
  namespace: platform
type: Opaque
stringData:
  secret: local-dev-jwt-secret-not-for-production-use
---
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: platform
type: Opaque
stringData:
  oidc-encryption-key: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
  internal-secret: local-dev-platform-internal-secret-please-rotate
---
# Stalwart admin creds mirror. Stalwart's own Secret lives in `mail` (used
# by its own config via %{env:ADMIN_SECRET}%), but platform-api runs in
# `platform` and k8s Secrets can't be referenced cross-namespace. So we
# keep a same-namespace copy of the cleartext here and the rotation
# endpoint updates both simultaneously. Initial value must match
# k8s/overlays/dev/stalwart/secret.yaml ADMIN_SECRET_PLAIN (dev only).
apiVersion: v1
kind: Secret
metadata:
  name: platform-stalwart-creds
  namespace: platform
type: Opaque
stringData:
  ADMIN_SECRET_PLAIN: stalwart-dev-admin
---
# oauth2-proxy config — base/oauth2-proxy excludes its own secret.yaml
# so each overlay must provide one with environment-specific URLs.
# OAUTH2_PROXY_COOKIE_SECRET must be exactly 16/24/32 bytes (here: 32 hex chars = 16 bytes).
apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-config
  namespace: platform
type: Opaque
stringData:
  OIDC_ISSUER_URL: "http://dind.local:2015/dex"
  OAUTH2_PROXY_CLIENT_ID: "hosting-platform-oauth2-proxy"
  OAUTH2_PROXY_CLIENT_SECRET: "local-dev-secret-oauth2-proxy"
  OAUTH2_PROXY_COOKIE_SECRET: "FCg1fX56K3KKIwfTwwTizNK1D6rLacez"
  OAUTH2_PROXY_REDIRECT_URL: "http://dind.local:2018/oauth2/callback"
